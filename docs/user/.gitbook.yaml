# Git Sync configuration for the "Documentation" section of Wyne Docs.
# The synced root is docs/user/ (set as the monorepo root directory in GitBook).
root: ./

structure:
  readme: README.md
  summary: SUMMARY.md
